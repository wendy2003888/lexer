flex filename  //generate a lex.yy.c file

cc -o <name> lex.yy.c -ll 
//change <name> in what name of .exe do u want delete "<>"

./name< filename
//name is the name of .exe u nominate just before, filename is the name of
file that u want 2 trans

